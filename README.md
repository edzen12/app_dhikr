# flutter_first_app

A new Flutter project.

<!-- // ОСНОВНЫЕ ВИДЖЕТЫ FLUTTER 

// Виджеты (Widget) - это строительные блоки Flutter.
// Любое приложение полностью состоит из виджетов.
// Каждый виджет может содержать внутри другие виджеты ("матрешка").

// runApp() - запускает Flutter приложение.

// MaterialApp - корневая обертка всего приложения.
// Здесь обычно настраиваются:
// - темы
// - маршруты (навигация)
// - локализация
// - главный экран (home)

// Scaffold - каркас одного экрана.
// Обычно содержит:
// - AppBar
// - Body
// - Drawer
// - BottomNavigationBar
// - FloatingActionButton

// SafeArea - защищает контент от вырезов экрана (челки),
// системных кнопок и статус бара.

// ===РАСПОЛОЖЕНИЕ ВИДЖЕТОВ ===

// Column - располагает виджеты сверху вниз.

// Row - располагает виджеты слева направо.

// Expanded - занимает всё оставшееся свободное место
// внутри Row или Column.

// Padding - внутренние отступы вокруг дочернего виджета.

// Margin задается через Container.margin
// и делает внешние отступы.

// Alignment - выравнивание содержимого внутри Container.

// SizedBox - создает пустое пространство
// либо задает фиксированную ширину/высоту.

// === КОНТЕЙНЕРЫ ===

// Container - универсальный блок.
// Может задавать:
// - размеры
// - цвет
// - отступы
// - выравнивание
// - декорацию
// - границы
// - скругления

// BoxDecoration - оформление Container.
// Здесь задаются:
// - цвет
// - границы
// - тень
// - borderRadius
// - изображения
// - градиенты

// BorderRadius - скругление углов.

// === ТЕКСТ ===

// Text - отображает текст.

// TextStyle - оформление текста:
// - размер
// - цвет
// - жирность
// - высота строки
// - семейство шрифта

// === ПРОКРУТКА ===

// ListView - прокручиваемый список.

// ListView.builder - создает элементы только тогда,
// когда они появляются на экране.
// Используется для длинных списков.

// itemCount - количество элементов списка.

// itemBuilder - функция,
// которая создает каждый элемент списка.

// === ВЗАИМОДЕЙСТВИЕ ===

// GestureDetector - делает любой виджет
// реагирующим на нажатия, свайпы и другие жесты.

// onTap - обработчик обычного нажатия.

// === ДИАЛОГИ ===

// showDialog() - показывает всплывающее окно.

// CupertinoAlertDialog - диалог в стиле iOS.

// TextButton - обычная текстовая кнопка.

// CupertinoTextField - текстовое поле в стиле iOS.

// === ИЗОБРАЖЕНИЯ ===

// SvgPicture.asset() - отображает SVG изображение
// из папки assets.

// === ПОЗИЦИОНИРОВАНИЕ ===

// MainAxisAlignment - выравнивание элементов
// по главной оси Row или Column.

// CrossAxisAlignment - выравнивание
// по поперечной оси.

// === СПЕЦИАЛЬНЫЕ ЗНАЧЕНИЯ ===

// double.infinity - занять всю доступную ширину.

// Colors - готовые цвета Flutter.

// const - объект создается один раз,
// что улучшает производительность.

// === build() ===

// build(BuildContext context)
// Метод, который строит интерфейс.
// Flutter вызывает его каждый раз,
// когда экран нужно перерисовать.

// BuildContext - информация о положении виджета
// внутри дерева приложения.
// Через context можно:
// - открыть диалог
// - перейти на другой экран
// - получить тему
// - получить размеры экрана

// === StatelessWidget ===

// StatelessWidget - неизменяемый виджет.
// После создания сам ничего не изменяет.
// Используется для статического интерфейса.

// StatefulWidget - виджет с состоянием.
// Может изменяться во время работы приложения
// через setState(). -->